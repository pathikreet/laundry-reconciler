## 2026-02-22 - Micro-Delight via Status Containers
**Learning:** Sequential `st.info` messages create anxiety and clutter. `st.status` provides a calm, progressive disclosure pattern that builds confidence for complex backend processes.
**Action:** Replace multi-step `st.info` sequences with `st.status` for all operations > 2 seconds.
